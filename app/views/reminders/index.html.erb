<p style="color: green"><%= notice %></p>

<h1>Reminders</h1>

<div id="reminders">
  <% @reminders.each do |reminder| %>
    <%= render reminder.becomes(Reminder) %>
    <p>
      <%= link_to "Show this reminder", reminder.becomes(Reminder) %>
    </p>
  <% end %>
</div>

<%= link_to "New reminder", new_reminder_path %>
